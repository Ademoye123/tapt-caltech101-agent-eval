# TAPT-Caltech101-AgentEval

Task package for the TAPT (CVPR 2025) Caltech101 robust-eval reproduction.

Large files are stored as `.part` chunks (GitHub 100MB file cap). Restore them and rebuild the submission zip:

```
bash reassemble.sh --zip
```
